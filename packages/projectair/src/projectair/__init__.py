"""projectair: the CLI wrapper that ships the `air` command alongside `airsdk`.

The forensic primitives live in ``airsdk``. This package glues them to the
``air`` terminal entry point.
"""
__version__ = "1.3.1"
