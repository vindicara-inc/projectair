// Ambient declaration so svelte-check accepts the `three` import used by the
// FlightDeck console's decorative Core3D background. Swap for `@types/three`
// if full Three.js typings are wanted.
declare module 'three';
