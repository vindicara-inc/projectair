# Stage 0 feasibility skeleton (runs anywhere)
numpy>=2.5.1
cryptography>=42.0.0

# Stage 1+ (real pipeline, NVIDIA GPU) — install when you move off synthetic data:
# monai>=1.3            # pathology/imaging encoders
# torch>=2.2            # fusion network on GPU
# nvflare>=2.4          # federated training across centers
# projectair            # the real Vindicara signing substrate (replaces trust.py)
